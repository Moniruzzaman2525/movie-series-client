import { HeroSkeleton } from "@/components/Shared/skeletons/hero-skeleton";


const loading = () => {
    return (
        <div>
            <HeroSkeleton />
        </div>
    );
};

export default loading;
