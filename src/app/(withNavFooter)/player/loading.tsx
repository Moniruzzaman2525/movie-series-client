import React from 'react';
import LoadingSpinner from '../movies/loading';

const loading = () => {
    return (
        <div>
            <LoadingSpinner/>
        </div>
    );
};

export default loading;