const TComment = {
        id: String,
        userId: String,
        videoId: String,
        comment: String,
      status: String,
      reviewId: String,
      parentCommentId: String,
        createdAt: String,
}

export default TComment